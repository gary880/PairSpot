apiVersion: v1
kind: Service
metadata:
  name: pairspot-api
  labels:
    app: pairspot-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: pairspot-api
